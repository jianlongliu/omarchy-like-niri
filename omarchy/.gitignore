# 第三方/社区插件（各带自己的上游 git 仓库），不进本配置仓库。
# 只跟踪我们自己克隆改动的 jianlongliu.* 插件（下方 ! 放行）。
plugins/*
!plugins/jianlongliu.*

# 嵌套 git 仓库兜底
.git/
**/.git/

# omarchy 自动生成的备份（*.bak.* 与 *.bak 都挡）
*.bak
*.bak.*
*.bak.*[0-9]
*.flat.*
**/backups/

# 日志
*.log

# 私人壁纸与头像（不进公开仓库）
backgrounds/
avatar.png
avatar_circle.png

# 私人锁屏素材（不进公开仓库）
# 注意：lock-avatar.png 例外——它是本仓库**有意收录的样例**（见 README），
# 已跟踪，gitignore 不影响已跟踪文件。仅用于表示"本机私有的同名文件不进仓库"。
lock-avatar.png
lock-videos/
